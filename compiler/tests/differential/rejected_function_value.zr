// `fn(int) -> int` can be written in a signature, and neither backend can run
// one. Both refuse it, and now say why.
fn twice(n: int) -> int {
    return n * 2;
}
fn apply(f: fn(int) -> int, n: int) -> int {
    return f(n);
}
fn main() {
    print(apply(twice, 5));
}
